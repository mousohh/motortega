import 'package:flutter/material.dart';

class MecanicoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Panel Mecánico")),
      body: Center(child: Text("Bienvenido Mecánico")),
    );
  }
}
